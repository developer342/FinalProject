<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.INTERNET" />

  <application
      android:allowBackup="true"
      android:dataExtractionRules="@xml/data_extraction_rules"
      android:fullBackupContent="@xml/backup_rules"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:networkSecurityConfig="@xml/network_security_config"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.FinalApp"
      android:usesCleartextTraffic="true"
      tools:targetApi="31">
    <activity
        android:name=".appui.HomeActivity"
        android:exported="false" />
    <activity
        android:name=".ledger.parse.ui.OcrInputActivity"
        android:exported="false" />
    <activity
        android:name=".ledger.camera.LedgerCameraActivity"
        android:exported="false" />
    <activity
        android:name=".user.ui.MeActivity"
        android:exported="false" />
    <activity
        android:name=".auth.ui.SignupActivity"
        android:exported="false" />
    <activity
        android:name=".auth.ui.LoginActivity"
        android:exported="false" />
    <activity
        android:name=".appui.MainActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider.ledger"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths_ledger" />
    </provider>
  </application>

</manifest>